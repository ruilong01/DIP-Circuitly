version: 1.0
runtime: nodejs22

build:
  commands:
    build:
      - npm install --prefix backend

run:
  runtime: nodejs22
  command: node backend/server.js
  network:
    port: 3000
  env:
    - name: NODE_ENV
      value: "production"
